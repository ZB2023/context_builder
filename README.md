# 🏗️ Context Builder

Консольный инструмент для сканирования, экспорта и управления структурой проектов.

---

## 📋 Возможности

- 📁 **Сканирование директорий** — три режима: одиночный, множественный, рекурсивный
- 📄 **Экспорт** — в форматы TXT, Markdown, JSON
- 🔄 **Конвертация** — между форматами через сохранённую сессию
- 🔒 **Цензура** — автоматическая замена паролей, API-ключей, email, IP-адресов
- 🌳 **Tree View** — визуализация дерева файлов (вкл/выкл)
- 🔢 **Подсчёт токенов** — оценка размера для LLM (GPT-4 и др.)
- 📋 **Буфер обмена** — копирование результата одной командой
- ✂️ **Разбиение** — автоматическое разделение больших отчётов на части
- ⚙️ **Профили** — сохранение и загрузка пресетов настроек
- 🖥️ **CLI режим** — запуск без интерактивного меню для автоматизации
- 🗑️ **Удаление** — безопасное удаление отчётов с подтверждением
- 🔍 **Фильтрация** — выбор файлов по расширению или имени

---

## 🚀 Установка

### 1. Клонируй репозиторий

```bash
git clone https://github.com/ТВОЙ_ЛОГИН/context-builder.git
```

```bash
cd context-builder
```

### 2. Создай виртуальное окружение

```bash
python -m venv venv
```

### 3. Активируй окружение

Windows (PowerShell):

```bash
venv\Scripts\Activate.ps1
```

Linux / macOS:

```bash
source venv/bin/activate
```

### 4. Установи зависимости

```bash
pip install -r requirements.txt
```

# 💻 Использование

## Интерактивный режим

```bash
python main.py
```

### Откроется меню с навигацией стрелками:

→ Сканирование (Запись)
&nbsp; Конвертация
&nbsp; Переконвертация
&nbsp; Удаление записи
&nbsp; Выбор файлов в директориях
&nbsp; Настройки
&nbsp; Выход

### CLI режим

# Базовое сканирование

```bash
python main.py --path "./my_project" --format txt --output report
```

# С цензурой

```bash
python main.py --path "./src" --format md --redact --output secure_report
```

# Только предпросмотр

```bash
python main.py --path "./src" --preview
```

# Без дерева структуры

```bash
python main.py --path "./src" --format txt --no-tree
```

# Разбиение на части по 2 МБ

```bash
python main.py --path "./src" --format txt --split 2 --output big_report
```

# Тихий режим (для скриптов)

```bash
python main.py --path "./src" --format json --silent --output auto_report
```

### CLI параметры

|Параметр           |Описание                        |По умолчанию       |
|:------------------|:-------------------------------|:------------------|
|--path             |Путь к директории (обязательный)|-                  |
|--output           |Имя файла без расширения        |scan_ДАТА          |
|--format           |Формат: txt, md, json           |txt                |
|--output-dir       |Директория для сохранения       |Сканируемая        |
|--no-tree          |Не включать дерево структуры    |Выкл               |
|--redact           |Включить цензуру данных         |Выкл               |
|--max-file-size    |Лимит размера файла (МБ)        |10                 |
|--split            |Разбить на части по N МБ        |0                  |
|--silent           |Тихий режим                     |Выкл               |
|--preview          |Только предпросмотр             |Выкл               |


# 🔒 Безопасность
## Цензура конфиденциальных данных
### Программа умеет находить и заменять:

|Тип|Пример|Результат|
|-|-|-|
|API Key|api\_key = "abc123..."|\*\*\*REDACTED\*\*\*|
|Пароль|password = "secret"|\*\*\*REDACTED\*\*\*|
|Email|user@example.com|\*\*\*REDACTED\*\*\*|
|IPv4|192.168.1.1|\*\*\*REDACTED\*\*\*|
|AWS Key|AKIA1234567890AB|\*\*\*REDACTED\*\*\*|
|Private Key|-----BEGIN PRIVATE KEY-----|\*\*\*REDACTED\*\*\*|
|Connection String|mongodb://user:pass@host|\*\*\*REDACTED\*\*\*|

# Локальная песочница

* Все данные хранятся только локально
* Программа не отправляет данные в интернет
* Сессии сохраняются в скрытой папке .context\_builder/
* Рекомендуется добавить .context\_builder/ в .gitignore

# 📁 Структура проекта

context\_builder/

├── main.py                    # Точка входа

├── pyproject.toml             # Метаданные

├── requirements.txt           # Зависимости

├── .gitignore

├── README.md

├── LICENSE

├── src/

│   ├── \_\_init\_\_.py

│   ├── menu.py                # Интерактивное меню

│   ├── scanner.py             # Сканирование

│   ├── session.py             # JSON-сессии

│   ├── exporter.py            # Экспорт (TXT/MD/JSON)

│   ├── converter.py           # Конвертация

│   ├── redactor.py            # Цензура

│   ├── preview.py             # Предпросмотр

│   ├── config.py              # Профили

│   ├── clipboard.py           # Буфер обмена

│   ├── chunker.py             # Разбиение

│   ├── token\_counter.py       # Токены

│   ├── cli.py                 # CLI

│   └── utils/

│       ├── \_\_init\_\_.py

│       ├── file\_filter.py     # Фильтр бинарников

│       ├── filename.py        # Имена файлов

│       ├── encoding.py        # Кодировки

│       └── safety.py          # Безопасность

# 🛠️ Требования

* Python 3.10+
* Windows / Linux / macOS

# Зависимости

|Библиотека|Назначение|
|-|-|
|InquirerPy|Интерактивное меню|
|rich|Красивый вывод в консоль|
|fpdf2|Генерация PDF|
|pyperclip|Буфер обмена|
|tiktoken|Подсчёт токенов|
|chardet|Определение кодировки|
